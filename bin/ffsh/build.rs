/// Builds the SYSTEM-3 `libc` implementation and places the built
/// artifact into the build directory.
fn main() -> io::Result<()> {
   return Ok(());
}

// MODULES ////////////////////////////////////////////////////////////////////////////////////////

use std::io;
